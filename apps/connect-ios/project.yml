name: EKKOConnect
options:
  bundleIdPrefix: app.ekkoconnect
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "15.0"
  createIntermediateGroups: true

packages:
  Supabase:
    url: https://github.com/supabase/supabase-swift.git
    from: "2.0.0"
  Kingfisher:
    url: https://github.com/onevcat/Kingfisher.git
    from: "7.0.0"
  RevenueCat:
    url: https://github.com/RevenueCat/purchases-ios-spm.git
    from: "5.0.0"

targets:
  EKKOConnect:
    type: application
    platform: iOS
    sources:
      - path: EKKOConnect
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: app.ekkoconnect.connect
        MARKETING_VERSION: "2.0.6"
        CURRENT_PROJECT_VERSION: "1"
        DEVELOPMENT_TEAM: ""  # set in Xcode → Signing & Capabilities
        INFOPLIST_FILE: EKKOConnect/Resources/Info.plist
        CODE_SIGN_ENTITLEMENTS: EKKOConnect/Resources/EKKOConnect.entitlements
        CODE_SIGN_ALLOW_ENTITLEMENTS_MODIFICATION: YES
        SWIFT_VERSION: "5.9"
        TARGETED_DEVICE_FAMILY: "1"
        IPHONEOS_DEPLOYMENT_TARGET: "17.0"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        GENERATE_INFOPLIST_FILE: false
    dependencies:
      - package: Supabase
        product: Supabase
      - package: Kingfisher
      - package: RevenueCat
        product: RevenueCat
      - target: EKKOConnectWidgets
        embed: true
        codeSign: true

  # Unit tests, hosted by the EKKOConnect app so `@testable import EKKOConnect`
  # resolves the app's internal symbols (models, extensions, pure helpers).
  # xcodegen sets TEST_HOST + BUNDLE_LOADER from the `- target: EKKOConnect`
  # dependency below.
  EKKOConnectTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: EKKOConnectTests
    dependencies:
      - target: EKKOConnect
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: app.ekkoconnect.connect.tests
        GENERATE_INFOPLIST_FILE: true
        SWIFT_VERSION: "5.9"
        IPHONEOS_DEPLOYMENT_TARGET: "17.0"
        TARGETED_DEVICE_FAMILY: "1"

  # WidgetKit extension that owns the "New match" Live Activity. Shares
  # MatchActivityAttributes with the app target via an explicit source entry
  # so both processes decode the same schema.
  EKKOConnectWidgets:
    type: app-extension
    platform: iOS
    sources:
      - path: EKKOConnectWidgets
      - path: EKKOConnect/Core/LiveActivity/MatchActivityAttributes.swift
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: app.ekkoconnect.connect.widgets
        MARKETING_VERSION: "2.0.6"
        CURRENT_PROJECT_VERSION: "1"
        INFOPLIST_FILE: EKKOConnectWidgets/Info.plist
        SWIFT_VERSION: "5.9"
        TARGETED_DEVICE_FAMILY: "1"
        IPHONEOS_DEPLOYMENT_TARGET: "17.0"
        SKIP_INSTALL: YES
        GENERATE_INFOPLIST_FILE: false
